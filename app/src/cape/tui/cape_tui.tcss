/* Cape TUI Stylesheet */

Screen {
    align: center middle;
    background: $surface;
}

ModalScreen {
    background: $surface-darken-2 50%;
}

#content {
    height: 1fr;
    margin: 0 0 2 0;
    width: auto;
}

#modal_content {
    background: $surface;
    height: 75%;
    margin: 0 0 2 0;
    padding: 1;
    width: 75%;
}

/* Header and Title */
#content_header {
    border-bottom: double $accent;
    content-align: left middle;
    color: $text;
    margin: 2 0 1 0;
    padding: 0 1;
    text-style: bold;
    width: 100%;
}

#content_footer {
    border-top: double $accent;
    width: 100%;
}

#modal_header {
    border-bottom: double $accent;
    content-align: left middle;
    color: $text;
    margin: 0 0 1 0;
    padding: 0 1;
    text-style: bold;
    width: 100%;
}

/* DataTable Styling */
DataTable {
    height: 1fr;
    width: auto;
}

DataTable > .datatable--header {
    background: $surface;
    color: $text;
    text-style: bold;
}

DataTable > .datatable--cursor {
    background: $accent-muted 35%;
}

/* Form Styling */
Input, TextArea {
    background: $surface-darken-1;
}

TextArea {
    width: 100%;
}

.divider {
    color: $surface-lighten-2 50%;
    margin: 1 1;
}

/* Status Colors */
.status-pending {
    color: $warning;
}

.status-started {
    color: $accent;
}

.status-completed {
    color: $success;
}

/* Modal Screens */
ModalScreen {
    align: center middle;
}

#create-issue-modal {
    width: 75%;
    height: 50%;
    background: $surface;
    padding: 0 1;
}

#description {
    height: 1fr;
    margin-bottom: 1;
}

#button-row {
    width: 100%;
    height: auto;
    align-horizontal: center;
}

Button {
    margin: 0 1;
}

/* Detail Screen */
#detail-container {
    height: 1fr;
    padding: 1 2;
}

#detail-header {
    width: 100%;
    height: 3;
    content-align: center middle;
    text-style: bold;
    background: $primary;
    color: $text;
    margin-bottom: 1;
}

#issue-content {
    height: auto;
    margin-bottom: 2;
    padding: 1;
    border: solid $primary;
}

#comments-header {
    width: 100%;
    height: 3;
    content-align: left middle;
    text-style: bold;
    background: $accent;
    color: $text;
    margin-bottom: 1;
}

#comments-log, #comments-widget {
    height: auto;
    border: solid $primary;
    padding: 1;
    overflow-y: hidden;
}

Comments {
    height: auto;
    border: solid $primary;
    padding: 1;
    overflow-y: hidden;
}

/* Workflow Screen */
#workflow-container {
    height: 1fr;
    padding: 1 2;
}

#workflow-title {
    width: 100%;
    height: 3;
    content-align: center middle;
    text-style: bold;
    background: $primary;
    color: $text;
    margin-bottom: 1;
}

#workflow-progress {
    width: 100%;
    height: 3;
    content-align: center middle;
    background: $accent;
    color: $text;
    margin-bottom: 1;
}

.log {
    height: 1fr;
    overflow-y: scroll;
    border: solid $primary;
    padding: 1;
}

#workflow-log {
    height: 1fr;
    border: solid $primary;
    padding: 1;
}

/* Edit Description Modal */
#edit-description-modal {
    width: 98%;
    height: 95%;
    border: solid $primary;
    background: $surface;
    padding: 0 1;
}

/* Confirm Delete Modal */
#confirm-delete-modal {
    width: 70;
    height: 20;
    border: thick $error;
    background: $surface;
    padding: 1 2;
}

#issue-preview {
    width: 100%;
    padding: 1;
    margin-bottom: 1;
    border: solid $primary;
    background: $panel;
}

#delete-warning {
    width: 100%;
    content-align: center middle;
    padding: 1;
    margin-bottom: 1;
    color: $error;
    text-style: bold;
}

/* Help Screen */
#help-modal {
    width: 100;
    height: 40;
    border: thick $primary;
    background: $surface;
    padding: 1 2;
}

#help-content {
    width: 100%;
    height: 100%;
    overflow-y: scroll;
}

/* IssueForm Widget */
IssueForm {
    height: 1fr;
}

/* Collapsible Widget for Description */
Collapsible {
    margin-bottom: 2;
}

Collapsible > Contents {
    padding: 1;
}

/* Workflow sections */
#workflows-header {
    width: 100%;
    height: 3;
    content-align: left middle;
    text-style: bold;
    background: $accent;
    color: $text;
    margin-bottom: 1;
    margin-top: 1;
}

#workflows-content {
    height: auto;
    margin-bottom: 2;
    padding: 1;
    border: solid $primary;
}
