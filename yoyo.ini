[DEFAULT]
sources = migrations/
migration_table = _yoyo_migration
batch_mode = on

# Database connection is set via environment variable
# Example: DATABASE_URL=postgresql://user:pass@host:port/dbname
# Or use: yoyo apply --database postgresql://user:pass@host:port/dbname
