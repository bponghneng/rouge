# Trigger Phrases

Use `git-ops` when requests are similar to:

- "compose commits"
- "group and commit my changes"
- "create conventional commits from current diff"
- "prepare a pull request"
- "prepare a merge request"
- "push my branch and open PR"
- "commit everything and create a PR"
- "commit + MR workflow"
