# -----------------------------------------------------------------------------
# Rouge Application Suite - Environment Variables
# Copy this file to `.env` in the `rouge/` directory and fill in the values.
# -----------------------------------------------------------------------------

# === Required: Supabase project credentials ===================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# === Optional: Supabase HTTP client settings ==================================
# HTTP request timeout in seconds (defaults to 30)
# SUPABASE_HTTP_TIMEOUT=30

# Whether to verify SSL certificates (defaults to true)
# SUPABASE_HTTP_VERIFY=true

# === Required: Claude / Anthropic access for workflow automation =============
# Used by `rouge.core.agent` when executing the Agent Development Workflow.
ANTHROPIC_API_KEY=sk-your-anthropic-api-key

# === Optional: Agent provider selection =======================================
# Which provider to use for the /implement step (defaults to "claude")
# Options: "claude", "opencode"
# ROUGE_IMPLEMENT_PROVIDER=claude

# Which provider to use for general agent operations (defaults to "claude")
# Options: "claude", "opencode"
# ROUGE_AGENT_PROVIDER=claude

# === Optional: Claude CLI / Agent execution settings =========================
# Path to the Claude CLI executable (defaults to `claude` if not set).
# CLAUDE_CODE_PATH=/usr/local/bin/claude

# Whether the Claude CLI should keep the working directory stable (default: true).
# CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR=true

# Claude Code sandbox API key (only needed if you use e2b cloud sandboxes).
# E2B_API_KEY=

# Personal access token for GitHub; also used as GH_TOKEN for Claude Code.
# Required for automatic PR creation in workflow. Must have repo scope.
# Requires gh CLI to be installed and available in PATH.
# GITHUB_PAT=

# Personal access token for GitLab.
# Required for automatic MR creation in workflow when DEV_SEC_OPS_PLATFORM=gitlab.
# Must have api scope. Requires glab CLI to be installed and available in PATH.
# GITLAB_PAT=

# Platform for DevSecOps operations (PR/MR creation).
# Options: "github", "gitlab"
# If not set, PR/MR creation step will be skipped in the workflow.
# DEV_SEC_OPS_PLATFORM=

# === Optional: OpenCode CLI settings =========================================
# Path to opencode executable (defaults to "opencode")
# OPENCODE_PATH=/usr/local/bin/opencode

# API key for OpenCode (if required by provider)
# OPENCODE_API_KEY=your-opencode-api-key

# === Optional: File-system locations =========================================
# Override where agent logs/prompts are stored (defaults to ./.rouge/logs/agents).
# ROUGE_AGENTS_DIR=/absolute/path/to/agents/logs

# Override the base data directory used for runtime/state/log files.
# ROUGE_DATA_DIR=/absolute/path/to/.rouge

# === Optional: Workflow execution settings ====================================
# Repository root path used by CodeRabbit review generation (defaults to current directory)
# REPO_PATH=/absolute/path/to/repository/root

# Working directory for agent commands and CodeRabbit execution (defaults to current directory)
# WORKING_DIR=/absolute/path/to/working/directory

# Allow destructive git operations (git reset --hard) in workflow setup step.
# Set to "true" to enable. Required for workflow execution but will discard uncommitted changes.
# WARNING: Only enable this in worker environments or when you're sure you want to reset git state.
# ROUGE_ALLOW_DESTRUCTIVE_GIT_OPS=true

# === Optional: Logging ========================================================
# Log level for Rouge output (defaults to INFO)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# ROUGE_LOG_LEVEL=INFO

# === Optional: Worker settings ================================================
# Workflow execution timeout in seconds (defaults to 3600)
# Controls how long a workflow is allowed to run before timing out.
# ROUGE_WORKFLOW_TIMEOUT_SECONDS=3600
